services:
  postgres:
    image: postgres:16-alpine
    container_name: autoclean-manager-postgres
    environment:
      POSTGRES_DB: autoclean_manager
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d autoclean_manager"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  api:
    build:
      context: .
      dockerfile: AutocleanManager.Api/Dockerfile
    container_name: autoclean-manager-api
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__DefaultConnection: Host=postgres;Port=5432;Database=autoclean_manager;Username=postgres;Password=postgres
    ports:
      - "8080:8080"

volumes:
  postgres_data: